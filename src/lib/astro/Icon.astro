---
import { Icon as Component } from '@daks.dev/astro.sdk/astro/icon';
import type { IconKeys } from '$app/icons';

type Props = {
  icon: IconKeys;
} & Record<string, unknown>;

const { icon, ...props } = Astro.props;
---

<Component
  {icon}
  {...props}
/>
